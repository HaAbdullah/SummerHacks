# SummerHacks

1. Env files (once):
   - Windows: `.\scripts\setup-env.ps1`
   - macOS/Linux: `./scripts/setup-env.sh`
2. Backend: `pip install -r requirements.txt` → `uvicorn app.main:app --reload --port 8000`
3. Frontend: `npm install` → `npm run dev`
